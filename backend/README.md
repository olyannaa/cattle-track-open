# Cattle Track Backend

ASP.NET Core API for Cattle Track.

Основная документация находится в [корневом README](../README.md). Production-конфигурация и секреты должны передаваться через переменные окружения или секреты CI/CD.

## Docker Build

```bash
docker build -t cattle-track-backend-local ./CAT
```
